services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: cia
      POSTGRES_PASSWORD: cia
      POSTGRES_DB: cia
    ports:
      - "5432:5432"
    volumes:
      - cia_pgdata:/var/lib/postgresql/data

volumes:
  cia_pgdata:
