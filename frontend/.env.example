# Leave unset in local dev: the Nuxt dev server proxies /api → FastAPI (no CORS issues).
# Set to your API root if you bypass the proxy, e.g. http://127.0.0.1:8000
# NUXT_PUBLIC_API_BASE=http://localhost:8000
